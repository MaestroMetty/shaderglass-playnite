<UserControl x:Class="ShaderGlass.ShaderGlassSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Executable Path -->
            <TextBlock Text="{DynamicResource LOCShaderGlassExecutablePathLabel}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassExecutablePathDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="{DynamicResource LOCShaderGlassBrowseButton}" Click="BrowseExecutableButton_Click" Padding="10,5"/>
            </Grid>

            <!-- Profiles Path -->
            <TextBlock Text="{DynamicResource LOCShaderGlassProfilesPathLabel}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassProfilesPathDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ProfilesPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="{DynamicResource LOCShaderGlassBrowseButton}" Click="BrowseProfilesButton_Click" Padding="10,5"/>
            </Grid>

            <!-- Launch Delay -->
            <TextBlock Text="{DynamicResource LOCShaderGlassLaunchDelayLabel}" FontWeight="Bold" Margin="0,15,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassLaunchDelayDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <TextBox Text="{Binding LaunchDelaySeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Width="80" HorizontalAlignment="Left"/>

            <!-- Launch delay mode -->
            <TextBlock Text="{DynamicResource LOCShaderGlassAutoLaunchDelayModeLabel}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassAutoLaunchDelayModeDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <RadioButton Content="{DynamicResource LOCShaderGlassAutoLaunchWithDelay}" IsChecked="{Binding AutoLaunchWithDelay}" Margin="0,0,0,5"/>
            <RadioButton Content="{DynamicResource LOCShaderGlassAutoLaunchWithoutDelay}" IsChecked="{Binding AutoLaunchWithoutDelay}" Margin="0,0,0,15"/>

            <!-- Notifications -->
            <TextBlock Text="{DynamicResource LOCShaderGlassShowNotificationsLabel}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassShowNotificationsDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <CheckBox Content="{DynamicResource LOCShaderGlassShowNotificationsCheckbox}" IsChecked="{Binding ShowNotifications}" Margin="0,0,0,15"/>

            <!-- Ignored Profiles -->
            <TextBlock Text="{DynamicResource LOCShaderGlassIgnoredProfilesLabel}" FontWeight="Bold" Margin="0,15,0,5"/>
            <TextBlock Text="{DynamicResource LOCShaderGlassIgnoredProfilesDescription}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <ListBox ItemsSource="{Binding ProfileItems, RelativeSource={RelativeSource AncestorType=UserControl}}" MaxHeight="200" Margin="0,0,0,15">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding ProfileName}" IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="{DynamicResource LOCShaderGlassRefreshProfiles}" Click="RefreshProfilesButton_Click" Padding="10,5" Margin="0,15,0,15"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>