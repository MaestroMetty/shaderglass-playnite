<UserControl x:Class="ShaderGlass.ShaderGlassSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Executable Path -->
            <TextBlock Text="{Binding ExecutablePath_Label}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding ExecutablePath_Description}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="{Binding BrowseButton_Label}" Click="BrowseExecutableButton_Click" Padding="10,5"/>
            </Grid>

            <!-- Profiles Path -->
            <TextBlock Text="{Binding ProfilesPath_Label}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding ProfilesPath_Description}" TextWrapping="Wrap" Margin="0,0,0,5" Foreground="Gray"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ProfilesPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="{Binding BrowseButton_Label}" Click="BrowseProfilesButton_Click" Padding="10,5"/>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>